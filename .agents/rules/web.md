---
trigger: always_on
glob:
description:
---
# Web Development Rules

- **CRITICAL**: You are NOT allowed to apply or make any changes to components inside the `ui` directory (e.g., `components/ui/*`) without first asking for explicit permission from the USER. This includes styling, logic, or structure changes.
